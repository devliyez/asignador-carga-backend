spring.application.name=asignarcarga

# Configuración de la Base de Datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/asignacioncarga
spring.datasource.username=${DBPost_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:abecedario}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true